#ans 1
when we need a specific element we use getElementById.
when we need all element by name specific class name we use getElementsByClassName.
when we need to sellect all matching elements using a CSS selector we use querySelectorAll.

#ans 2
I can create and insert a new element into the DOM to use document.createElement("").

#ans 3
Event Bubbling is a mechanism in the js DOM where an event starts from the target element and going up through its child ans going up through its parent elements until it reaches the document.

#ans 4
Event Delegation is a technique where i  adding eventlisteners to many child elements, i can add one eventlistener to their parent, and handle events using event bubbling.

#ans 5
when we need to stop the default behavior of an element we use preventDefault()
when we need to stop the event from bubbling up to parent elements we use stopPropagation()

